

<% include partials/headerAngular %>
<div class="container">
    <div class="row text-center" >
            <div ng-repeat="book in data.books | filter:query">
                    <div class="col-md-3 col-sm-6 catalog">

                        <img class = " img-catalog" ng-src="{{book.image}}" alt="{{book.name}}"> 
                          <a href="/AllBooks/{{book._id}}" class="btn btn-lg btn-block btn-primary">{{book.price}}
                          </a>  
                         
                    </div>
                </div> 
                      
            </div>
    
    </div>  
</div>
        
    
<% include partials/footer %>

               